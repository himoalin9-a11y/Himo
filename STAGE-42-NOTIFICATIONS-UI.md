# Stage 42 - Notifications and UI

- Persist pending notification conversation IDs across cold-start initialization.
- Retry notification deep-link navigation after the MAUI Shell/window is ready.
- Keep Android FCM notification channel initialization available during cold start.
- Refresh the main conversation screen, chat composer, login, and search styling.
- Application logic for messaging/attachments/audio is unchanged.

Validation required in Visual Studio: Release / Any CPU Rebuild Solution with 0 errors and 0 warnings.
