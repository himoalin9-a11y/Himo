Himo v244 – Stage 1: UI redesign foundation + login screen

Scope:
- Established a cleaner premium Himo login visual direction.
- Redesigned the login screen layout, hierarchy, spacing, cards, inputs, buttons, and server-status panel.
- Preserved all existing x:Name fields and Clicked handlers used by LoginPage.xaml.cs.
- No authentication, API, database, navigation, or business logic changes.

Next stage:
- Redesign the conversations/home screen using the same visual language, without changing its bindings or event handlers.
