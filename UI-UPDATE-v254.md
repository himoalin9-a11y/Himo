# Himo v254 — UI Stage 2: Conversations

- Redesigned Home/Conversations header to a clean light layout.
- Kept server/connection controls hidden from the Home UI.
- Kept existing navigation, refresh, search, polling, notifications, and conversation selection handlers unchanged.
- Server configuration remains only in Settings (v253).
- No Render, Supabase, Brevo, API, authentication, or SignalR code changed.
