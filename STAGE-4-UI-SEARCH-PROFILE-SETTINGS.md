Himo Stage 4 – Search, Profile, Settings UI

- Redesigned SearchPage visually while preserving event handlers and bindings.
- Redesigned ProfilePage visually while preserving names/events.
- Removed the server/API connection section from Settings UI; backend/API configuration remains in code and is not exposed to normal users.
- No chat transport, authentication, database, or SignalR logic changed.
