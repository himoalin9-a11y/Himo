Himo v242 - Redesigned Conversations UI

Replaced the previous HomePage visual layout with a distinct modern conversations interface while preserving existing event handlers, bindings, data model, navigation, refresh, notifications, and server logic.
